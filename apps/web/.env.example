# Application
NEXT_PUBLIC_APP_NAME=Nova
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LOCALE=en

# API — the backend's real global prefix is /v1, not /api (confirmed against the
# running backend's route table; this was previously wrong here).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/v1
