# Nova

Nova is split into two packages:

- `frontend/` for the React application
- `backend/` for the API server, to be implemented next

The repo is being scaffolded in two phases so the frontend can move first without blocking the backend structure.

## Workspace Layout

```text
frontend/   React + Vite + TypeScript app
backend/    Node.js API scaffold
```